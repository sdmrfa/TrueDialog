name: Deploy opt-out-td-webhook to Azure Function App

on:
  push:
    branches:
      - sdmrf/revamp
  workflow_dispatch:

jobs:
  build:
    name: Build Azure Function (opt-out-td-webhook)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'

      - name: Install pnpm
        run: npm install -g pnpm

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile
        working-directory: .

      - name: Build Worker
        run: pnpm --filter=opt-out-td-webhook build
        working-directory: .

      - name: Prune Dependencies
        run: npm prune --production
        working-directory: apps/workers/opt-out-td-webhook

      - name: Prepare Deployment Package
        run: |
          mkdir deploy
          cp -r apps/workers/opt-out-td-webhook/dist deploy/dist
          cp apps/workers/opt-out-td-webhook/host.json deploy/
          cp apps/workers/opt-out-td-webhook/package.json deploy/
          cp -r apps/workers/opt-out-td-webhook/node_modules deploy/node_modules
          cd deploy && zip -r ../release.zip .
        working-directory: .

      - name: Upload Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: opt-out-td-webhook
          path: release.zip

  deploy:
    name: Deploy to Azure Function App
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Download Build Artifact
        uses: actions/download-artifact@v4
        with:
          name: opt-out-td-webhook
          path: .

      - name: Unzip Package
        run: unzip -o release.zip -d deploy

      - name: Deploy to Azure Function App
        uses: Azure/functions-action@v1
        with:
          app-name: 'tdrgotd'
          publish-profile: ${{ secrets.AZURE_OTD_PUBLISH_PROFILE }}
          package: ./deploy
          sku: flexconsumption
          remote-build: false