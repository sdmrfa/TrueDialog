name: Deploy opt out hubspot webhook to Azure Function App

on:
  push:
    branches:
      - sdmrf/revamp
  workflow_dispatch:

jobs:
  build:
    name: Build Azure Function
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'

      - name: Install pnpm
        run: npm install -g pnpm

      - name: Install dependencies
        run: pnpm install --filter=opt-out-hs-webhook... --frozen-lockfile

      - name: Build Function App
        run: pnpm --filter=opt-out-hs-webhook build

      - name: Archive Function App
        run: |
          mkdir deploy
          cp -r apps/workers/opt-out-hs-webhook/dist/* deploy/
          cp apps/workers/opt-out-hs-webhook/package.json deploy/
          cd deploy && zip -r ../function-release.zip .

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: function-app
          path: function-release.zip

  deploy:
    name: Deploy to Azure Function App
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: function-app
          path: .

      - name: Unzip artifact
        run: unzip -o function-release.zip -d deploy

      - name: Deploy to Azure Function App
        uses: Azure/functions-action@v1
        with:
          app-name: 'tdrgohs'
          package: './deploy'
          publish-profile: ${{ secrets.AZURE_OHS_PUBLISH_PROFILE }}
